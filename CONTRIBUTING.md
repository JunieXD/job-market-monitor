# 贡献指南

## 新增数据源

每个连接器必须：

1. 只访问公开且无需登录的招聘页面。
2. 输出统一 `JobRecord`，不直接操作数据库。
3. 使用稳定的来源岗位 ID。
4. 支持分页完整性检查和数量上限保护。
5. 将一个岗位的多个城市建模为列表。
6. 区分岗位直接分类和官网筛选成员关系，允许多分类和未分类，不得补造默认分类。
7. 保存参与解析的分类/组织条件响应，并为原始字段生成覆盖率统计。
8. 提交解析测试和完全虚构或充分脱敏的 fixture。
9. 明确记录数据源 URL、访问频率和已知限制。

## 禁止提交的数据

Pull Request 不得包含真实批量招聘数据、原始网页、浏览器 Cookie、账号信息、数据库文件或数据库导出。测试 fixture 应尽量人工构造；确需保留页面结构时必须删除真实描述、地址、人员和跟踪信息。

## 来源字段与派生字段

连接器只负责来源事实。官网直接提供但尚未进入公共模型的字段必须保留在原始快照中，并在
数据契约确定后再规范化。通过正则、词典、统计模型或 LLM 得到的学历、经验、技能、专业和
业务线等信息，未来必须进入单独模块和数据表，不得写回 `JobRecord` 的原始字段。

## 本地检查

```bash
.venv/bin/pytest -q
.venv/bin/ruff check .
```
