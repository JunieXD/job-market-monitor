# 安全策略

## 支持范围

当前只支持 `main` 分支中的最新代码。项目仍处于早期开发阶段，部署前请在隔离环境验证数据库
迁移和采集范围。

## 报告问题

如果发现可能导致凭据泄露、未授权数据访问、命令注入或原始数据越权暴露的问题，请不要在公开
Issue 中直接发布利用细节。请通过 GitHub 的私下安全报告功能联系维护者，或先通过项目维护者的
公开联系方式取得安全沟通渠道。

报告中请包含：

- 受影响的文件、命令或配置；
- 可复现的最小步骤；
- 影响范围和建议修复方式；
- 是否已经在真实服务器或真实招聘数据上验证。

请不要提交数据库密码、访问令牌、Cookie、真实岗位批量数据或包含个人信息的原始响应。

## 数据使用边界

项目只面向公开招聘信息观测，不收集求职者个人信息，不使用登录态或绕过招聘网站访问控制。运行
数据、原始响应、数据库导出和服务器凭据均属于部署环境私有数据，不能提交到 GitHub。
